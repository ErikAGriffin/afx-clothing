var app = angular.module('afxClothing');app.service('$cart',function($http){  var self = this;  self.addToCart = function(cartObject) {    $http({      url:'/addToCart',      method: "POST",      headers: {'Content-Type': 'application/x-www-form-urlencoded'},      transformRequest: function(obj) {        var str = [];        for (var p in obj) {          str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));        }        return str.join("&");      },      data: cartObject    })    .error(function(data,status) {      console.log(status+": Error adding item to cart.\n"+data);    });  };  self.removeFromCart = function(cartObject) {    $http({      url:'/removeFromCart',      method: "POST",      headers: {'Content-Type': 'application/x-www-form-urlencoded'},      transformRequest: function(obj) {        var str = [];        for (var p in obj) {          str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));        }        return str.join("&");      },      data: cartObject    })    .error(function(data,status) {      console.log(status+": Error removing item from cart.\n"+data);    });  };});