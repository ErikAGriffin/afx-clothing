(function() {  var app = angular.module('afxClothing');  var ngPost = function($http, url, object) {    $http({        url: url,        method: "POST",        headers: {'Content-Type': 'application/x-www-form-urlencoded'},        transformRequest: function(obj) {          var str = [];          for (var p in obj) {            str.push(encodeURIComponent(p) + "=" + encodeURIComponent(obj[p]));          }          return str.join("&");        },        data: object      })      .error(function(data,status) {        console.log(status+": Error posting data to "+url+".\n"+data);      });  };  app.service('$cart',function($http){    var self = this;    self.addToCart = function(cartObject) {      ngPost($http,'/addToCart', cartObject);    };    self.removeFromCart = function(cartObject) {      ngPost($http,'/removeFromCart',cartObject);    };    self.updateQuantity = function(cartObject) {      ngPost($http,'/updateQuantity', cartObject);    };  });}());